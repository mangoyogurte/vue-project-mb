
``` bash
# install dependencies
npm install

# 打包dll（dependencies更新，dll需重新打包）
npm run dll

# 本机调试
npm run dev

# 测试服打包并上传（需修改build/test.sh内容）
npm run test

# 正式服打包
npm run build

# build for production and view the bundle analyzer report
npm run build --report
```

